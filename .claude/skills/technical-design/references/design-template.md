# Design document template

Copy into `docs/02-design/02-technical/{YYYYMMDD}-{NN}-{slug}.md`.

`status`: `ร่าง` → `ใช้งาน` → `superseded`
`spec`: wikilinks to the source specs — never empty; a design with no
requirement behind it should not exist.

---

```markdown
---
title: <หัวข้อภาษาไทยสั้น ๆ>
date: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
status: ร่าง
spec: ["[[../../01-requirements/01-spec/20260802-01-table-self-ordering|20260802-01]]"]
adr: []
related: []
supersedes: []
---

# <หัวข้อภาษาไทย>

## 1. ขอบเขตของเอกสารนี้

ออกแบบส่วนใดของระบบ และ **ไม่ได้** ออกแบบส่วนใด (ชี้ไปเอกสารที่รับผิดชอบส่วนนั้นแทน)

อ้างอิงจาก: <wikilink ไปสเปคต้นทาง>
สถานะสเปคต้นทาง ณ เวลาที่ออกแบบ: <ร่าง / ใช้งาน> — <คำถามที่ค้างกระทบการออกแบบหรือไม่>

## 2. ภาพรวมสถาปัตยกรรม

องค์ประกอบหลักและการไหลของข้อมูลระหว่างกัน ระบุให้ชัดว่าอะไรคุยกับอะไรด้วยวิธีใด

```mermaid
flowchart LR
  ลูกค้า[มือถือลูกค้า] --> เว็บ[หน้าเว็บสั่งอาหาร]
  เว็บ --> api[API]
  api --> ฐานข้อมูล[(ฐานข้อมูล)]
  api --> จอครัว[หน้าจอบาริสต้า]
```

## 3. เทคโนโลยีที่ใช้

| ส่วน | เลือกใช้ | เหตุผลสั้น ๆ | ADR |
| --- | --- | --- | --- |
| <เช่น Backend> | <ชื่อ + เวอร์ชัน> | <เหตุผล> | <wikilink หรือ —> |

การตัดสินใจที่มีน้ำหนักและมีเอกสารอื่นอ้างถึง ให้แยกเป็น ADR ไม่เขียนยัดในตารางนี้

## 4. Database schema

ระบุชื่อตารางและคอลัมน์จริง ชนิดข้อมูลจริง และข้อจำกัดจริง
เขียนแบบที่หยิบไปสร้างตารางได้เลย

### ตาราง `<ชื่อตาราง>`

| คอลัมน์ | ชนิด | ข้อจำกัด | คำอธิบาย |
| --- | --- | --- | --- |
| `id` | `uuid` | PK | |

**ความสัมพันธ์:** <ตารางใดอ้างถึงตารางใด และลบต่อเนื่องหรือไม่>
**Index ที่ต้องมี:** <คอลัมน์ใด เพราะ query แบบใด>

## 5. API contract

### `<METHOD> <path>`

**วัตถุประสงค์:** <ทำอะไร>
**สิทธิ์ที่ต้องมี:** <ใครเรียกได้>

Request

```json
{ }
```

Response — `200`

```json
{ }
```

| รหัส | กรณี | ข้อความที่ส่งกลับ |
| --- | --- | --- |
| `400` | <เมื่อไร> | |
| `409` | <เมื่อไร> | |

## 6. การไหลของงานสำคัญ

ลำดับขั้นของกรณีที่ผิดพลาดได้ง่าย เช่น จ่ายเงินแล้วระบบล่ม
ระบุว่าใครรับผิดชอบการกู้คืน และสถานะค้างกลางทางถูกจัดการอย่างไร

## 7. การตรวจสอบย้อนกลับ (Traceability)

**ต้องครบ** — เอกสารนี้เป็นฐานของ [[../../03-testing/01-test-plan/index|01-test-plan]]

| ข้อกำหนด | องค์ประกอบที่รองรับ | หมายเหตุ |
| --- | --- | --- |
| BR-1 (`01`) | | |
| AC-1 (`01`) | | |

**สรุปความครอบคลุม:** BR <n>/<total> · AC <n>/<total>

ข้อกำหนดที่ยังไม่ครอบคลุมในเอกสารนี้ — ระบุว่าอยู่ในเอกสารใด หรือยังไม่มีใครรับ

| ข้อกำหนด | อยู่ที่ไหน |
| --- | --- |

## 8. ที่ยังตัดสินใจไม่ได้

ต้องว่างก่อนเปลี่ยน `status` เป็น `ใช้งาน`

- [ ] <ประเด็น — กระทบส่วนใดของการออกแบบ และรอคำตอบจากใคร>

## 9. สิ่งที่ต้องกลับไปเพิ่มใน requirement

สิ่งที่การออกแบบต้องใช้ แต่ไม่มีสเปคใดระบุไว้ — **ห้ามคิดข้อกำหนดขึ้นเองในเอกสารนี้**
ให้ยกกลับไปทำเป็น requirement ก่อน

- [ ] <สิ่งที่ขาด — ควรอยู่ในสเปคฉบับใด>

## 10. เอกสารอ้างอิง

- [[../../01-requirements/01-spec/index|01-spec]]
- [[../../03-testing/01-test-plan/index|01-test-plan]]
- [[../../05-log/index|05-log]]
```
